# Database initialization - disabled to prevent deployment failures
# Run manually after deployment if needed:
# eb ssh
# source /var/app/venv/*/bin/activate
# cd /var/app/current
# python scripts/init_db.py init

# container_commands:
#   01_migrate:
#     command: "python scripts/init_db.py init"
#     leader_only: true
#     ignoreErrors: true